# Windows 禁用显卡驱动更新指南

在 Windows 系统中，有时自动更新会安装不兼容或有问题的显卡驱动。以下是几种禁用显卡驱动自动更新的方法。

## 方法一：使用组策略编辑器 (推荐 Windows 专业版/企业版)

此方法最稳定，可以精确阻止特定硬件 ID 的驱动更新。

1.  **获取显卡硬件 ID**：
    *   右键点击“开始”按钮，选择“设备管理器”。
    *   展开“显示适配器”，右键点击你的显卡，选择“属性”。
    *   切换到“详细信息”选项卡。
    *   在“属性”下拉菜单中选择“硬件 ID”。
    *   复制第一行的值（例如：`PCI\VEN_10DE&DEV_1C03...`）。

2.  **打开组策略**：
    *   按下 `Win + R` 键，输入 `gpedit.msc` 并回车。

3.  **配置策略**：
    *   导航到：`计算机配置` -> `管理模板` -> `系统` -> `设备安装` -> `设备安装限制`。
    *   双击右侧的 **“阻止使用与下列设备安装类相匹配的驱动程序安装设备”** (或者更精确的是 **“阻止安装与下列设备 ID 相匹配的设备”**)。
    *   选择 **“已启用”**。
    *   点击“显示”按钮。
    *   在弹出的窗口中，粘贴步骤 1 中复制的硬件 ID。
    *   点击“确定”保存所有更改。

4.  **生效**：
    *   重启电脑即可生效。之后 Windows Update 将无法为该显卡安装驱动。

---

## 方法二：使用注册表编辑器 (适用于 Windows 家庭版)

家庭版没有组策略编辑器，可以通过修改注册表实现类似效果。

**警告**：修改注册表有风险，请提前备份。

1.  **获取硬件 ID** (同方法一)。

2.  **打开注册表**：
    *   按下 `Win + R`，输入 `regedit` 并回车。

3.  **导航路径**：
    *   前往：`HKEY_LOCAL_MACHINE\SOFTWARE\Policies\Microsoft\Windows\DeviceInstall\Restrictions\DenyDeviceIDs`
    *   如果 `DeviceInstall`、`Restrictions` 或 `DenyDeviceIDs` 键不存在，请右键点击父键 -> 新建 -> 项，按层级创建它们。

4.  **添加限制**：
    *   在 `Restrictions` 键下，确保有一个名为 `DenyDeviceIDs` 的项。
    *   同时在 `Restrictions` 键下，右键空白处 -> 新建 -> DWORD (32位) 值，命名为 `DenyDeviceIDsRetroactive`，并将数值数据设为 `0` (如果希望已安装的不被卸载) 或 `1` (如果希望应用到已安装设备，但这可能会导致当前驱动失效，通常设为 0 即可，主要是为了阻止更新)。
    *   进入 `DenyDeviceIDs` 项。
    *   右键空白处 -> 新建 -> 字符串值。
    *   名称可以是 `1`，数值数据粘贴你的显卡硬件 ID。如果有多个 ID，可以依次命名为 `2`, `3` 等。

5.  **重启电脑**。

---

## 方法三：使用“显示或隐藏更新”工具 (微软官方工具)

微软提供了一个名为 `wushowhide.diagcab` 的工具，可以用来隐藏特定的 Windows 更新。

1.  从微软官方或可信来源下载 `wushowhide.diagcab`。
2.  运行工具，点击“下一步”。
3.  选择 **"Hide updates" (隐藏更新)**。
4.  在列表中勾选你要屏蔽的显卡驱动更新。
5.  点击“下一步”完成设置。

---

## 恢复更新

如果未来想要更新驱动：
*   **组策略/注册表方法**：将相应的设置改为“未配置”或删除注册表项。
*   **工具方法**：运行工具选择 "Show hidden updates" 并取消勾选。
